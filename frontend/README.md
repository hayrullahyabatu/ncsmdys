# Frontend

Bu dizin React.js uygulamasını içerir. Tailwind CSS kullanır.

## Başlatma

1. `npm install`
2. Yeni terminalde `npm run tailwind` komutuyla Tailwind çıktısını izleyin.
3. `npm start`

Uygulama `http://localhost:3000` adresinde çalışır.
